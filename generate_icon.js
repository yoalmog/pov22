const fs = require('fs');

// We have the svg! But we need a png. Let's just create a very simple 1x1 png 
// that we scale? Or actually let's just make the manifest accept icon.svg,
// Android Chrome DOES accept SVG if purpose ANY.
